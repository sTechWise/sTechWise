import type { Metadata } from "next";
export const metadata: Metadata = { title: "Customer Stories | sTechWise", description: "See how leading enterprises use sTechWise to automate workflows, reduce costs, and accelerate growth across healthcare, finance, retail, and more." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
