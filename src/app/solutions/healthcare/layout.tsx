import type { Metadata } from "next";
export const metadata: Metadata = { title: "Healthcare Automation Solutions | sTechWise", description: "Automate clinical workflows, claims processing, and compliance documentation while maintaining HIPAA compliance." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
