import type { Metadata } from "next";
export const metadata: Metadata = { title: "Finance Automation Solutions | sTechWise", description: "Automate invoice processing, reconciliation, compliance reporting, and financial workflows with enterprise AI." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
