import type { Metadata } from "next";
export const metadata: Metadata = { title: "Retail & E-Commerce Automation | sTechWise", description: "Automate inventory management, order fulfillment, dynamic pricing, and customer personalization for retail businesses." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
