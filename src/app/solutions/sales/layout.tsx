import type { Metadata } from "next";
export const metadata: Metadata = { title: "Sales Automation Solutions | sTechWise", description: "Automate lead routing, CRM updates, pipeline analytics, and deal acceleration for high-velocity sales teams." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
