import type { Metadata } from "next";
export const metadata: Metadata = { title: "HR Automation Solutions | sTechWise", description: "Automate onboarding, payroll, compliance, and employee lifecycle management for modern HR teams." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
