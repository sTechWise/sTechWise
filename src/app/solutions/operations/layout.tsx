import type { Metadata } from "next";
export const metadata: Metadata = { title: "Operations Automation | sTechWise", description: "Automate supply chain, procurement, resource planning, and operational workflows for enterprise operations teams." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
