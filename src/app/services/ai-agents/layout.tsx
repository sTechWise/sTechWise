import type { Metadata } from "next";
export const metadata: Metadata = { title: "AI Agents & Autonomous Systems | sTechWise", description: "Build and deploy autonomous AI agents that handle complex multi-step tasks with full observability and governance." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
