import type { Metadata } from "next";
export const metadata: Metadata = { title: "Business Automation Solutions | sTechWise", description: "End-to-end process automation across departments — procurement, compliance, contracts, and operations — with full audit trails." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
