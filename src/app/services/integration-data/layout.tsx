import type { Metadata } from "next";
export const metadata: Metadata = { title: "Integration & Data Automation | sTechWise", description: "300+ native connectors, real-time data pipelines, ETL/ELT orchestration, and API management for enterprise integration." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
