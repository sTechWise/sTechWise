import type { Metadata } from "next";
export const metadata: Metadata = { title: "AI Automation | sTechWise", description: "ML model orchestration, intelligent document processing, and predictive automation for enterprise workflows." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
