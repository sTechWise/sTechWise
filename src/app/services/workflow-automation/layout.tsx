import type { Metadata } from "next";
export const metadata: Metadata = { title: "Workflow Automation | sTechWise", description: "Visual workflow builder with RPA, process mining, conditional branching, and human-in-the-loop orchestration at enterprise scale." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
