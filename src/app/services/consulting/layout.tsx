import type { Metadata } from "next";
export const metadata: Metadata = { title: "Automation Consulting | sTechWise", description: "Strategy workshops, implementation sprints, training, and managed automation services from certified enterprise architects." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
